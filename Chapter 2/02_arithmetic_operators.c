#include <stdio.h>

int main()
{
  int a = 4;
  int b = 3;
  int c = a + b;
  printf("The value of a is %d and b is %d and sum is %d\n", a, b, c);
  printf("When a divided by b then remainder is %d\n", a % b);
  return 0;
}
